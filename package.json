{
  "name": "abiding-place-fellowship-cms",
  "version": "1.0.0",
  "description": "Abiding Place Fellowship - CMS-driven static site",
  "main": "scripts/build.js",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npx http-server public -p 8080",
    "validate": "node scripts/validate-content.js",
    "capture-screenshots": "node scripts/capture-screenshots.js",
    "test": "npm run validate && npm run build"
  },
  "dependencies": {
    "http-server": "^14.1.1",
    "marked": "^9.1.0",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
